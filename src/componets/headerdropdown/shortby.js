import React from 'react'

const shortby = (props) => {
    return (
        <>
        <div style={{left:"34px"}}  className={props.short ? "dropdownalltypes active" : "dropdownalltypes"}>
            
        </div>
        </>
    )
}

export default shortby